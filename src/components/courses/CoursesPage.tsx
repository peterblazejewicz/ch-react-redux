import React, { SFC } from 'react';

const CoursesPage: SFC<{}> = () => (
  <div className="jumbotron">
    <div className="container">
      <h1 className="display-3">Courses</h1>
    </div>
  </div>
);

export default CoursesPage;
